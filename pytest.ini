[pytest]
# Silence pytest-asyncio deprecation warning by pinning the default loop scope.
# This does not affect our current tests (they are synchronous), but keeps CI/validation clean.
asyncio_default_fixture_loop_scope = function
